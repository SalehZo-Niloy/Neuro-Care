import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center w-full min-h-[90vh]'>
            <button className="btn btn-square btn-lg loading text-primary"></button>
        </div>
    );
};

export default Loading;